# GHP-005: bounded learning and authored-thesis research

This is the pre-call implementation checkpoint. The operator subsequently approved
the external transfer and the bounded epoch completed. See the
[completed study and operational status](2026-09-21-jev-learning-results.md) for
the 4,526 calls, final feature freeze, thesis results and separate NinjaTrader
recovery. Approval-pending and zero-dispatch statements below describe this earlier
checkpoint, not current status.

The operator requested continuing improvement toward a coherent cognitive system.
This source-only increment adds a bounded question-selection engine, audited offline
API batch runner and causal thesis-replay primitives. It preserves the installed
0.0.2.93 payload, native code, cognition, cadence, AI/jobs and RECORD_ONLY observer.
No evidence, wake, classifier or exit authority is added to Hermes or Glitch.

## What is built and what has run

The learning engine uses three expanding 2024 folds, purges the forward label window
plus one day, and screens four independent Noul features per round for at most two
rounds. A change must improve mean 15/30/60-minute Brier by at least .002, improve two
of three folds, and avoid more than .003 damage on any horizon. This is development
selection, not evidence of an edge. A saved error report can guide the second proposal;
only development rows can enter that report. A final question freeze precedes opening
495 later original-training observations. Previously opened 2025-H2/2026 tests are
excluded from this development path; all historical confirmation remains development.

The local numerical baseline has run on 1,002 2024 observations. Mean Brier below is
equally weighted across three folds and three horizons. It is not economic expectancy.

| Frozen comparator | Mean development Brier |
| --- | ---: |
| 22-feature regularized logistic | 0.65490 |
| Training class frequency | 0.66400 |
| Depth-three compact numerical tree | 0.67295 |
| All-96-feature regularized logistic | 0.68420 |

The compact model's pooled 15/30/60-minute Briers are .64729/.64636/.67119. Its paired
differences from class frequency are respectively -.01681/-.01457/+.00455; all three
day-cluster 95% intervals cross zero. Fifteen and thirty minutes are better development
leads than sixty here. The small numerical sample also makes the all-feature model
more fragile; this does not overturn the distinct earlier large-sample results.

The thesis study reconstructs 1,261 usable positioned observations across the 80
master trades, forming 2,522 paired requests with/without authored text. Inputs carry
the original reason/invalidation, code-computed geometry, current observations and
up to ten previous marks. Terminal outcomes and actual subsequent Hermes rationale
exist only in separate local evaluation records. The endpoint and conditional-exit
evaluation driver is prepared; no new Jev results have been produced in this epoch.

Preflight found two M2K observations (2026-09-15 20:14 and 20:15 UTC) whose outer
contract was M2K 09-26 but embedded descriptive observations named M2K 12-26. They
are excluded from inference and probe-price histories. The initially prepared batch
is preserved under `rejected-preflight`, with no requests sent. This is additional
historical identity evidence, not proof of a current native execution defect.

## Provider approval state

Automatic approval review rejected the first external batch because proprietary
historical market-state data would be sent to TypeSafe without payload/destination
specific authorization. No API requests were dispatched. Explicit authorization is
pending for market/indicator observations and original Hermes theses sent to
`https://api.typesafe.ai/v1/systemone`, pinned `jev-1.13.0`, up to 5,500 calls and
$1.50 in local accounting across the bounded epoch. The actual prepared first-round
and thesis batches are 1,002 and 2,522 calls. Credentials, account/order identifiers,
future outcomes and private paths are excluded from reviewed inference states.

The request is for research data transfer only. It does not authorize native trading,
observer SHADOW activation, deployment or promotion. Round two remains dependent on
first-round results; confirmation remains dependent on the final question freeze.

## Validation and continuation

The full profile suite passes 782 tests, including 28 new tests for label-window
purging, feedback isolation, candidate retention, independent probability semantics,
future-state exclusion, contract identity, null/missing flow, persistence reset,
post-response fill availability, winner damage, API budgets, lock ownership, hash
binding and uncertain-dispatch suppression. Provider tests use a local fake client;
they do not satisfy the blocked real-provider experiment.

Epoch artifacts: `D:/ab/artifacts/glitch-jev-research/2026-09-19/learning-loop-v1`.
Read `PROTOCOL.md`, `payload-review.json`, `baseline-results.json` and
`embedded-contract-audit.json`. The prior two research seals remain unchanged.

After authorized external calls: score round one, write at most four revised/additional
questions from its development-only feedback, score round two, freeze the retained
set, then run the later confirmation exactly once. Evaluate the paired thesis study
including censored fills, original-winner damage and currently installed cognition
separately. Preserve every failed/rejected candidate and report missing or negative
results. No threshold or model may promote itself into the product.

GHP-001 remains deterministic perception; GHP-003/004 stop lines remain intact;
GHP-005 owns this research; GL-AI-10/11 remain native outcome/receipt truth. The live
observer remains RECORD_ONLY with zero inference requests. The missing Publisher and
fresh active-session acceptance gates remain unresolved. Source-only rollback is a
revert of this increment; there is no runtime setting to undo.

Preservation proof at 2026-09-20 03:20 UTC: 33/33 owned source/installed hashes match;
67 protected files and job definitions match the checkpoint; installed credentials
are unchanged; native DLL SHA-256 is unchanged. `FundamentalCache.tsv` refreshed
calendar/cache entries independently, with quote values unchanged apart from timestamps;
it is reported separately rather than falsely claimed byte-identical. Both prior
research seals verify (86 + 29 artifacts). This epoch has zero durable API dispatches.
