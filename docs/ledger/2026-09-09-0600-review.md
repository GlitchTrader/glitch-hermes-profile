# Six-hour review — 2026-09-09 06:00 Sao Paulo

Reviewed 03:00–09:00 UTC (00:00–06:00 local), including the v74/v75
deployment boundary at 03:13 UTC. Baseline main `241014a`, profile v75
`3682d1f88485`; native `18d3624`. AI and replication are ON. The native
worktree's unrelated web/documentation/package edits are excluded.

## Evidence

- Two new MNQ master round trips: long -$14.60 net, short +$38.90 net;
  master +$24.30, Sim102 +$48.10, Sim103 +$119.00, fleet +$191.40.
  These are two ideas, not six independent trades. Native SQLite executions
  corroborate the trade ledger. Master stop and target fills occurred at
  04:30:15.346633 and 06:27:36.886642 UTC respectively; ledger timestamps lag.
- Native initial stop/target receipts exist for the master and every follower
  partial fill. The earlier HOLD was checked before the stop fill; the later
  HOLD was correctly suppressed after the target fill. No model EXIT occurred.
- 191 decision attempts: 185 completed, five failed, one stale management
  correction correctly deferred. 31 successful attempts needed a correction;
  zero transport retries. Median total latency 53.1s; p95 109.2s. Every attempt
  attached a chart. These counts exclude separate learner calls.
- 168 NOTHING, 14 entry intentions, three HOLD. Ten entries were superseded
  by profile price revalidation and two by native price revalidation: only two
  became trades. Native M2K skips were at 2961.1 and 2961.4 outside the authored
  2960.7–2960.9 zone, not replication failures. Never widen an issued range.
- 26 correction requests were arithmetic verdict/range contradictions, of
  which 25 were NOTHING and one an entry. Three of the five final failures
  arose on those arithmetic retries (0335, 0406, 0715). Raw authored responses
  remain in the installed Hermes SQLite history.
- 0552 had a complete terminal audit tail plus an identical canonical
  final_choice. Normalization failed to relocate the other two authored fields;
  the resulting general correction then omitted required latency evidence.
- 0608 emitted one NOTHING per instrument instead of one shared master decision.
  Its format correction changed TRIGGER_REVIEW_V1 into INSTRUMENT_COMPARISON_V1;
  the correction prompt omitted the authoritative expected mode/book count.

## Smallest warranted release

Only direct-worker output preparation/correction context, focused tests and
release metadata. No SOUL/trading-skill, native AddOn, indicator, geometry,
probability, execution-range, safety, cadence, model or learner change.

1. For an already-authored NOTHING only, calculate a bare contradictory
   terminal EV label from the unchanged numeric levels, friction and probability
   range. This extends existing exact risk/reward/hurdle arithmetic; it neither
   chooses an action nor invents probability. Unknown/qualified evidence remains
   untouched. Entry and management contradictions retain strict validation and
   the existing one bounded repair. This is not making EV observational.
2. Relocate a complete terminal audit tail when its repeated final choice is
   identical to the existing canonical value. Conflicts and partial tails remain
   invalid; preserve all authored evidence verbatim.
3. Supply the existing expected ledger mode and ordered master count to the
   format correction. No new market analysis or additional retry.

Proof: witnessed failing tests, all profile regressions, offline replay of
preserved responses with no LLM calls, full diff review, source/remote/installed
parity, protected-data checkpoint and a natural fresh cycle. Rollback is the
checkpointed v75 distribution through the supported updater, never a reset.

## Still under investigation; not a profitability claim

Repeated worse-price WAIT reasoning and narrow entry zones remain concerning.
Some WAIT explanations claim lower short entries improve location, contrary to
the existing doctrine. A small positive window does not validate those claims.
Do not force entries, invent a dollar/ATR floor, or relabel every abstention a
defect. Review contemporaneous path/geometry and subsequent opportunity by
version. The long made favorable progress before its stop; a two-trade sample
cannot establish calibration, superior management or an enduring edge.

Deployment and final verification will be appended after proof, not assumed.

## Source and offline proof

- v76 changes only the direct worker's output preparation and correction context.
  Twenty-four new regression cases plus existing contracts pass: 438 total.
  Native/control verification passes 60 tests, including compiled production
  gateway/AddOn, configuration, lifecycle, recovery, protection, replication,
  partial fills and completed-bar harnesses. No live test order was submitted.
- Replay of all 26 preserved verdict-correction inputs changes only the bare
  arithmetic label on 25 NOTHING decisions. All other authored fields are
  identical. The remaining entry contradiction still requires strict correction.
  Three observed final failures would not enter their faulty second call.
- Actual 0552 replay now restores the existing disconfirmation/change condition
  verbatim and exposes the genuinely missing latency dimension to the existing
  single correction. It does not fabricate latency or admit an incomplete entry.
  Actual 0608 repair context now states one master and TRIGGER_REVIEW_V1; native
  count/mode validators remain unchanged. Future model compliance is not promised.
- At 09:23 UTC a naturally originated v75 MNQ short is open, with accepted stops
  and working targets on Sim101/102/103 in quantities 1/2/5. Deployment waits for
  a flat boundary instead of interrupting its management. NT PID remains 19808.
- The losing long's native execution extrema were 29593/29577 versus entry
  29583.75: gross favorable excursion $18.50 and stop loss $13.50. Its management
  packet only observed a $12.50 peak and explicitly labels sampling limits. The
  winner's native adverse excursion was $6.50 before its $40 gross target. Do not
  use later native extrema as though Hermes knew them in an earlier packet.
- The learner completed an 08:50 review without promoting a new lesson. Its
  current guidance recommends unchanged cognition; this is evidence of review,
  not evidence that it learned a profitable improvement. Subsequent learner
  deferrals to trading priority are visible, not counted as failed market calls.
