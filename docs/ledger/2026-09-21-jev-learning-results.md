# GHP-005: completed question development and authored-thesis replay

The authorized bounded epoch completed 4,526 direct TypeSafe requests for
$0.48566448 in local input-token accounting. Every response identified
`jev-1.13.0`; no result influenced Hermes or native trading. Both question rounds
completed, the final feature set was frozen, and the later confirmation ran once.
No new forecast feature met the predeclared retention rule. Authored-thesis
monitoring produced a small retrospective management lead with material winner
damage and wide uncertainty. The selected product authority remains Level 0.

Execution used source commit `ac29bc9ee64bdad1ee058061efca3ac4a41fcfbb`.
Artifacts are under
`D:/ab/artifacts/glitch-jev-research/2026-09-19/learning-loop-v1`.
Read `FINAL-RUN-AUDIT.json`, `round1-results.json`, `round2-results.json`,
`final-freeze.json`, `confirmation-results.json`, `thesis-results.json` and
`thesis-independent-audit.json`. The original protocol and all 33 pre-call inputs
remain unchanged; the preceding 86-file and 29-file research seals also verify.

## Forecast feature development

Each candidate was added to the same 22-feature numerical comparator on matched
2024 chronological folds. Negative Brier difference is better. Retention required
at least .002 mean improvement, improvement in two folds, and no horizon mean
damage above .003. The rule was not relaxed after seeing results.

| Candidate | Mean Brier difference | Improved folds | Retained |
| --- | ---: | ---: | --- |
| Upward pullback intact, round 1 | +.000069 | 2/3 | No |
| Downward pullback intact, round 1 | +.000345 | 1/3 | No |
| Upward expansion failing, round 1 | -.001701 | 3/3 | No |
| Downward expansion failing, round 1 | +.002906 | 2/3 | No |
| Upper extension rejected, round 2 | -.001092 | 2/3 | No |
| Upward pressure reemerging, round 2 | +.002033 | 0/3 | No |
| Downward pressure reemerging, round 2 | +.002386 | 0/3 | No |
| Two-sided equilibrium, round 2 | +.002264 | 0/3 | No |

Round two used the saved development-only error report, with its hash recorded in
the proposal. It did not improve on the best first-round relationship. All 1,002
requests in each round passed the typed checks. A zero-feature final freeze means
confirmation required no further Jev requests; it still evaluated the fixed
numerical comparators as specified.

| Comparator | 2024 development mean Brier | Later confirmation mean Brier |
| --- | ---: | ---: |
| Compact numerical logistic | .654898 | .666284 |
| Earlier training class frequency | .663995 | .666847 |
| Full 96-feature numerical logistic | .684204 | .703392 |
| Compact depth-three tree | .672952 | .676040 |

Confirmation contains 495 candidate observations from January through June 2025;
490/485/451 have eligible 15/30/60-minute score windows. Compact-model Brier is
.661820/.666199/.670835. Its differences from frequency are
-.007890/+.004784/+.001420; all day-cluster 95% intervals cross zero. These reused
historical periods remain development evidence. They are not a new untouched
holdout or proof of an economic edge.

The frozen sparse numerical economic probes were +$129.35, +$40.85 and +$165.25
across 24/34/40 observations at the three horizons. Coverage was only 4.9%/7.0%/8.9%,
with very different instrument results. These are numerical-only, separate
one-micro experiments, not Jev improvement or a continuously traded portfolio.
They do not authorize importing the fitted classifier into cognition or admission.

## Authored-thesis management study

The paired study evaluated 1,261 positioned marks across 80 native-matched master
trades and 14 cognition bundles. Current and prior causal observations were paired
with either the original Hermes thesis or a geometry-only state. The original
thesis materially affected interpretation: the geometry-only arm predominantly
answered UNKNOWN and never reached the frozen persistent deterioration signal.
This is appropriate abstention without a named thesis, not independent evidence
that the authored judgments are correct.

The primary signal required DETERIORATING plus FAILED probability at least .70 on
two consecutive usable publication minutes. No threshold search followed scoring.

| Primary authored-thesis probe | Result |
| --- | ---: |
| Trades with a signal | 26/80 |
| Signals with a later observable price before native closure | 18 |
| Signals without an eligible post-response price | 8 |
| Gross conditional advantage | +$100.50 |
| Advantage after extra exit costs | +$66.975 |
| Improved / harmed observable trades | 14 / 4 |
| Effect on original losing trades | +$165.375 |
| Effect on original winning trades | -$98.40 |
| Mean advantage per observable trade | +$3.721 |
| Day-cluster 95% interval for that mean | -$12.633 to +$11.598 |
| MES / MNQ advantage | +$35.675 / +$31.30 |
| Current cognition `79c8c74f09ce` | 4 signals; 2 observable; +$25.90 |

M2K's two signals were both censored by missing post-response prices. The strict
secondary FAILED-at-.70 probe produced no signals. Changing assumed spread/slippage
from 1.5 ticks to .5/2.5 ticks changes total primary advantage to +$82.725/+$51.225;
the .55-per-contract extra commission remains charged. On 72 uncensored paired
trades, authored-minus-geometry mean advantage is +$.930, with a day-cluster
interval of -$2.492 to +$3.900.

Hermes was already running at seven of the 26 signal times. Nineteen signals had
a subsequent recorded review: its median start was only 6.38 seconds later and
completion 44.19 seconds later. Thus these minute marks do not establish a large
additional wake benefit. Faster detection before the next minute requires actual
prospective subminute observations. A hypothetical faster exit is also different
from a wake followed by Hermes choosing an action.

The five-minute endpoint forecasts remain weak. On the same 1,109 eligible paired
endpoints, Brier was .748520 with the thesis and .752415 without it; the uniform
three-class reference is .666667. Thesis interpretation and endpoint prediction
must therefore be evaluated separately. There are no independent human labels for
thesis validity, and minute marks cannot establish intraminute stop priority.

## Provider and audit findings

Median round-one/thesis/round-two latency was 510/505/619 ms from this host.
The thesis batch had 2,510 accepted and 12 malformed responses under the pinned
client contract, all HTTP 200. The twelve failures named a Choice other than the
maximum-probability option. Two additional near-tie thesis answers were excluded by
the stricter downstream check. Exclusions broke persistence; none was retried.
TypeSafe's [Choice contract](https://docs.typesafe.ai/primitives/choice) defines the
selected option as the highest-probability option. Raw responses remain preserved.

The independent audit recomputed the economic totals and matched endpoint sample.
It also records a reporting erratum: the generic bootstrap helper's text says
negative favors the first model. For exit advantage, positive favors the earlier
exit; for authored-minus-geometry advantage, positive favors the authored arm.
Numbers and intervals were not altered. A separate initial profile-manifest check
compared hexadecimal letter case incorrectly; `profile-parity-20260921-v2.json`
corrects it and verifies all 33 source/installed manifest files.

## Architecture consequence and operational continuation

The implemented learning mechanism can propose, measure, reject and freeze
questions; it does not train Jev's weights. TypeSafe's [current model documentation](https://docs.typesafe.ai/models)
confirms shared model weights and adaptation through state/questions and downstream
models. This epoch does not justify a forecast classifier, direct entry, reversal,
or direct exit in the product.

The next research focus is a shadow thesis-consistency observer using the exact
authored invalidation and fresh same-contract state. It should measure detection
lead, duplicate reviews, winner damage and missing evidence against unchanged
Hermes behavior. A later wake proposal must show that it offers useful review time
before the existing minute management cycle. The current five-minute flat scan and
native execution, protection, replication and reconciliation remain the fallback.
No implementation or authority for that later wake proposal is selected here.

The operator separately authorized live-market SIM validation, then requested
AddOn removal, an operator restart, full redeployment and recompilation to diagnose
NinjaTrader startup. On September 21 at 01:03 UTC, the exact installed 95-file
`AddOns/GlitchAddOn` folder was backed up, hash-verified and removed. All six
indicators match authored source; the two raw differences are generated wrappers.
The canonical AddOn source compiled successfully against installed NinjaTrader
8.1.8.1 dependencies (86 compilation inputs). The compiled native DLL was preserved;
source compilation does not prove native reload or successful startup.

Recovery evidence is at
`D:/ab/artifacts/glitch-nt-recovery/20260921T010224Z/RECOVERY.md`. Following the
operator's PC reboot, the native endpoints recovered. The supported full-folder
deployment restored all 95 AddOn files at 02:17 UTC with exact source hashes.
NinjaTrader's shared native DLL was rebuilt at 02:17:50 UTC. This proves a new file
on disk; the loaded assembly's MVID was not exposed by the runtime and is not
claimed verified. All six indicators match authored source and were not redeployed.
Active cache publication now carries `Publisher` on all twelve bridge readings.
The PC crash cause remains unresolved; the recovery report separates the Windows
events, a queued graphics live dump, and the missing fatal crash dump.

The supported `ai-auto on` operation resumed the unchanged SIM operator and learner
jobs. At 02:30 UTC, all three native endpoints were healthy, the selected executor
was Sim101, existing replication was effective, and Sim101/102/103 were flat with
zero working orders. By 02:32:51 UTC, seven reviews had completed: five NOTHING
receipts and two MNQ ENTER_LONG proposals superseded before posting. Those two
reviews took 46.17 and 106.34 seconds. Their current asks, 30087.00 and 30086.50,
exceeded the authored range ceilings of 30086.25. The existing revalidation and
bounded reassessment path operated; no entry range, cadence or authority changed.
These are useful timing observations, not completed-trade or replication-fill
acceptance.

A hidden RECORD_ONLY process resumed at 02:21 UTC, with zero API calls, zero API
budget, a four-hour duration limit and a 128-MB disk limit. Its first 126
observations span 11.56 minutes: median interval 5.032s, p95 6.037s, maximum 8.041s,
zero duplicate identities and zero read errors. M2K primary/descriptive state was
stale in 75 observations; MES in six. Recording every five seconds therefore does
not establish fresh five-second information for every instrument. This gap is
preserved in eligibility evidence. The recorder used about 25 MB of RAM; the host
had about 50 GB available at the bounded check. No paid live Jev SHADOW requests
were started, and longer prospective label/calibration acceptance remains open.

GHP-005 remains in progress. GHP-001 stays deterministic perception; GHP-003/004
stop lines remain intact. GL-AI-10/11 remain native outcome/receipt truth. No native
source change or installed cognition replacement was made by this research epoch.

The completion changes are documentation only. JSON and scoped-diff preservation
checks pass. The current Rail CLI reports missing `claim.assignee` and
`claim.claimed_at` on GHP-005 and GHP-001 in both the unchanged HEAD ledger and the
updated ledger. This pre-existing validation failure is recorded rather than
silently changing ownership of adjacent work. The earlier 782-test source result
is not represented as a fresh full-suite run after the PC crash.
