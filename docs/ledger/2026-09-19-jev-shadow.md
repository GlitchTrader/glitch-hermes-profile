# GHP-005 — Level 0 source, installation and recorder acceptance

The operator resolved the Level 0 architecture on September 19, 2026. Profile v0.0.2.93 now contains the isolated recorder/provider path described in [the operating contract](../jev-shadow-research.md). GHP-005 remains **in progress** because active-market input acceptance and prospective forecast evaluation have not happened.

Implemented and published on canonical `main`: `211f96b` (observer), `fccdadb` (timestamp continuity through malformed generations), `38e34d9` (Windows read sharing). All production payload hashes below correspond to `38e34d9f105b628fdef4dd0c152432f16a611c39`. Subsequent ledger-only commits do not change the installed payload.

- Full profile suite: **724 passed**, including 44 observer tests. The native-sharing test uses `ReplaceFileW`, the operation behind GlitchStateStore's .NET `File.Replace`; Python's `os.replace` exercises a different Windows operation. Replacement succeeds while the shared read handle retains the old complete bytes. A second test verifies the capture loop retries a replacement race and records only the new complete generation.
- One authorized synthetic provider smoke call returned exact `jev-1.13.0`, valid answers and 1,203ms process-inclusive latency, with estimated cost $0.000063084. This proves transport, not market prediction.
- Verified checkpoint: **73 files, 294,771,846 bytes**, including installed owned files, configuration/jobs/memories, supervisor learning evidence, native TSVs, control/epoch and outcomes. Credentials stayed in their original files and were checked for preservation without being copied into the payload. The preserved session database is outside updater ownership and was neither reset nor copied.
- Supported `hermes profile update glitch --yes` and installed `setup.ps1` completed. **33/33 manifest files** match source and installation. **67/67 protected files** remained byte-identical; the native fundamental quote cache separately refreshed only quote timestamps, with every other value identical. Both cron job identities, schedules and paused state stayed unchanged. AI remained paused. Config, authentication and learner state passed preservation checks.
- Native verification: **95 AddOn files and six indicator files** remain unchanged against the research baseline; the on-disk Custom DLL hash is unchanged. NinjaTrader's existing September 15 process was not restarted. The loaded managed assembly identity remains independently unproven; installed-source/disk parity is not that proof.
- At `2026-09-19T23:27Z`, the installed hidden process was running **RECORD_ONLY**, with zero API requests and no error. The exact **29,557 cache bytes** survived recording and three process epochs as **one distinct observation**. Unchanged weekend bytes did not generate fake samples. The observer uses about 26MiB working set in this initial check. No service or automatic restart was installed.

The live cache remains stale and lacks explicit publisher identity. Those are recorded input limitations, not waived gates. All seven native accounts were flat with no working orders at the verification snapshot. Native replication reported effective while the persisted desired flag was false; both pre-existing control surfaces were preserved rather than reconciled by this work.

Detailed local evidence: `D:/ab/artifacts/glitch-jev-research/2026-09-19/implementation-v93/` contains the checkpoint manifest, parity report, synthetic transport receipt and `final-validation.json`. This directory is outside the product payload. Recorder health is `GlitchData/jev-shadow/health.json`; create `GlitchData/jev-shadow/STOP` to stop only this process. Evidence is capped at 512MiB with no automatic deletion. The stopped/started process checks exercised this control without touching NinjaTrader or scheduled cognition.

The remaining gate is naturally active SIM data: realized cache cadence, exact native contracts, publisher identity, native event freshness and raw fidelity. Only then may an explicitly frozen SHADOW experiment start. There is no wake, packet-evidence, entry, exit or reversal authority, no fitted research model installed and no profitability claim. GHP-001/003/004 and GL-AI-10/11 boundaries are preserved.
