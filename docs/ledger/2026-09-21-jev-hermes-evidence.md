# GHP-006: Jev evidence reached an ordinary Hermes SIM review

The operator requested implementation of regime, reversal/continuation and future
direction evidence for Hermes. The resolved architecture is
[Level 2 SIM advisory evidence](../jev-hermes-evidence.md). The independent observer
now supplies eight typed distributions: regime, 15-minute continuation/reversal/
rotation, 15/30/60-minute endpoints, maturity, effort acceptance/absorption and,
when the original position is attributable, thesis condition. No Jev trading or
wake authority was selected.

## Source, installation and preservation

| Evidence | Verified result |
| --- | --- |
| Canonical implementation | `glitch-hermes-profile/main`, commit `34f4960f9288a149569bb51adea17e65125d4c83` |
| Published | GitHub `refs/heads/main` independently resolved to that implementation commit |
| Profile | `0.0.2.94`, supported `hermes profile update glitch --yes`, then installed setup |
| Installed integrity | 35/35 manifest hashes matched source |
| Cognition identity | `direct-v28-jev-advisory-evidence-b136ce66bb9e` |
| Regression tests | 809 passed; 31 scorer/label tests subsequently passed after a report-provenance clarification |
| Rail | Canonical ledger validation passed; GHP-006 explicitly reconciles the old GHP-001/005 boundary |
| Checkpoint | 119 files, 471,761,677 bytes copied with streaming SHA-256 verification |
| Preserved | Auth, config, learner epoch, account groups/overrides, runtime policy and native configuration hashes matched |
| AI/jobs | Both original job IDs and schedules retained; prior enabled state restored through supported control |
| Native | No NT source/build/indicator/workspace change or restart; unrelated 100-path native checkout diff preserved |

The code correction tolerates only floating-point noise at a tied maximum Choice
probability. Material selected-choice discrepancies remain invalid. Missing Rail
claims received current ownership. Historical sealed studies remain unchanged.
The observer's global single slot, per-contract rotation, two-second deadline,
supersession, durable budgets, STOP and reader freshness/identity checks were
tested. No synchronous provider call entered the Hermes worker.

## First completed connection proof

Trial launch: **2026-09-21 03:24:15 UTC**, PID **10508**, process epoch
`2026-09-21T032415406847Z-b463fefe2d59`. Direct TypeSafe is pinned to
`jev-1.13.0`. The frozen question hash is
`f268041f840b2725bfb675733e66a3f8c4fb7c66dfb1435e9027b38d512582fb`.

The normally scheduled cycle `20260921T0325Z` started at 03:25:34.025498 UTC and
completed at 03:27:29.495129 UTC. Its model attempt records
`included_in_prompt: true` for request `3d854c84af62492ebdeb25b8f68f2f80`.
The exact per-cycle context links to the observer's request and prediction,
with matching state hash and returned model. Hermes returned **NOTHING**. Glitch
accepted the intent, and its receipt reports `no_native_action_requested`.
This is a successful cognition/delivery test with no forced order or fill.
The following naturally admitted cycle also received evidence.

Prompt inclusion does not prove that Jev caused the choice; the final audit did
not explicitly attribute it to Jev. No trading edge, incremental decision benefit
or live position-thesis-management benefit is established by this connection.
The first full Hermes call took about 115.5 seconds: Jev's faster inference does
not remove Hermes's own decision latency.

At 03:28:17 UTC, the first 35 requests produced 32 valid responses, one material
selected-choice discrepancy and two deadline overruns. Twenty-three predictions
were eligible; nine responses were superseded, with overlap among exclusion
categories. Median valid-response end-to-end latency was 1,195.5 ms. Reported
input-token cost accounting was $0.01198134, excluding unknown usage from timed-out
calls; it is not a reconciled provider invoice. Invalid/late/superseded advice was
not offered. Source freshness remains intermittent, particularly for M2K; absence
does not turn into invented data or a directional signal.

The observer used approximately 26 MB of RAM. NT remained responsive at roughly
943 MB. API calls are serialized; no historical bulk inference or native compile
was started during this trial.

## Trial boundary and next evidence

The frozen run admits at most 120 calls, with $4 maximum conservative reservation
and a 15-second minimum cadence per explicit contract (MNQ/MES/M2K 12-26, Sim101).
After the inference budget, recording continues until its 4,500-second duration
or 128-MB total journal limit. The planned duration ends around **04:39:15 UTC**
on September 21. No recurring Jev activation was installed. Expired evidence is
omitted, while the original Hermes schedule and native protection continue.

Future endpoint scoring retains this question/state epoch, joins the raw native
observations and uses explicit neutral bands, timing tolerances and costs. The
scorer labels only horizons actually asked and keeps advisory influence unknown
until joined to consumer contexts/attempts. Its report is analysis-only; it does
not relabel underlying consumed predictions as untouched shadow evidence.
The historical studies still supplied no retained predictive feature under their
fixed rule; this advisory trial is a new prospective evaluation, not a rewrite
of those results. Position-thesis behavior is contract-tested but still needs
naturally occurring positioned SIM evidence. No direct Jev EXIT is enabled.

Local evidence root:
`D:/ab/artifacts/glitch-jev-research/2026-09-21/hermes-evidence-v1`.
It contains the pre-activation protocol, before-state, verified checkpoint
manifest, installation proof, resource/live snapshots and copied first-cycle
context/attempt/outbox/receipt with hashes. The private API environment file is
outside source and is never included in these reports.

Rollback is the independent `GlitchData/jev-shadow/STOP` file. It removes advisory
use immediately on the consumer's next read and stops the observer; no native
action is inferred. A profile rollback uses the supported installation procedure
and preserves all native and learner evidence.
